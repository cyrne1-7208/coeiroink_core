# COEIROINK kaldiio guard

ESPnetはTTS推論でもKaldiデータ入出力用の`kaldiio`を必須の依存関係として読み込みますが、COEIROINK Coreのモデル読み込みと音声合成では`ark`/`scp`機能を使用しません。

この独自実装はTTS推論に必要なインポート処理だけを成立させ、Kaldiデータ入出力APIが要求された場合は明示的な例外を送出します。上流kaldiioのコードは含みません。
